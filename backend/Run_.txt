rm -rf bin obj

dotnet clean SummitView_Functions.csproj
dotnet build SummitView_Functions.csproj


F5 = func start in vs code

When you come back rearrange files to avoid rebuild error, well at least try theory

That did not work try this: Explicitly list your functions in the .csproj so only those compile:

This along with adding these two lines seemed to work...will have to add to shadow market
<EnableDefaultCompileItems>false</EnableDefaultCompileItems> //No auto compile
<FunctionsEnableWorkerIndexing>false</FunctionsEnableWorkerIndexing> //avoid duplicate func error
